# Yazeed English — نظام Access Code مختلف لكل عميل

> **Frontend-Only Reference**
>
> هذا المستند يشرح كيفية تحويل نظام التفعيل الحالي من **Access Code ثابت لكل دورة** إلى **Access Code فريد لكل عميل**، مع الحفاظ على المشروع **Frontend-only بالكامل**.

---

## 1. النظام الحالي

حاليًا يوجد **Access Code ثابت لكل منتج**:

| المنتج | Access Code |
|---|---:|
| STEP | `111111` |
| English | `222222` |
| Trab6 | `333333` |
| Writing | `444444` |

### النظام الجديد

يصبح لكل عميل **Access Code خاص به**، ويمكن ربط الكود بأكثر من دورة:

| العميل | Order Number | Access Code | الدورات |
|---|---:|---:|---|
| العميل A | `212345678` | `583921` | STEP + English |
| العميل B | `212345679` | `741205` | Trab6 |
| العميل C | `212345680` | `936114` | Writing |

---

## 2. فصل ملفات العملاء

يفضل أن يكون لكل عميل ملف مستقل حتى لا يصبح لدينا ملف واحد ضخم يحتوي على جميع العملاء.

### البنية المقترحة

```text
hub.yazeedenglish.com/
│
├── index.html
├── style.css
├── script.js
├── access-guard.js
│
├── activate/
│   ├── index.html
│   ├── style.css
│   └── script.js
│
└── customers/
    ├── map.js
    ├── 212345678.js
    ├── 212345679.js
    └── ...
```

### مسؤولية كل مجلد

- **Hub files** → الواجهة والمنطق العام.
- **Activate files** → التحقق و`Consent` وإدارة عملية التفعيل.
- **Customers files** → بيانات العملاء فقط.
- **Course files** → محتوى الدورات.

---

## 3. محتوى ملف عميل واحد

مثال:

```text
customers/212345678.js
```

محتوى الملف:

```javascript
window.YAZEED_CUSTOMER = {
    orderNumber: "212345678",
    accessCode: "583921",
    courses: [
        "step",
        "english"
    ]
};
```

### مبدأ مهم

كل ملف عميل يحتوي على **بيانات ذلك العميل فقط**.

لا نضع داخل ملف العميل:

- CSS
- HTML
- منطق النظام
- وظائف التفعيل
- أكواد عامة للمشروع

---

## 4. عميل يملك دورة واحدة

إذا كان العميل يملك دورة واحدة فقط:

```javascript
window.YAZEED_CUSTOMER = {
    orderNumber: "212345679",
    accessCode: "741205",
    courses: [
        "trab6"
    ]
};
```

---

## 5. عميل يملك عدة دورات

يمكن للعميل امتلاك عدة دورات باستخدام نفس Access Code:

```javascript
window.YAZEED_CUSTOMER = {
    orderNumber: "212345680",
    accessCode: "936114",
    courses: [
        "step",
        "english",
        "trab6",
        "writing"
    ]
};
```

---

## 6. كيف يعرف النظام ملف العميل؟

يوجد ملف فهرس صغير:

```text
customers/map.js
```

مثال:

```javascript
window.YAZEED_CUSTOMER_FILES = {
    "212345678": "212345678.js",
    "212345679": "212345679.js",
    "212345680": "212345680.js"
};
```

### تسلسل البحث

```text
Order Number
      ↓
   map.js
      ↓
ملف العميل
      ↓
مطابقة Access Code
      ↓
   Consent
      ↓
   التفعيل
```

بهذه الطريقة لا يحتاج النظام إلى تحميل أو تحليل جميع ملفات العملاء في الوقت نفسه.

---

## 7. Access Code الفريد

في هذا التصميم يكون لكل عميل **Access Code خاص به**.

مثال:

```text
583921
```

وعميل آخر يمكن أن يمتلك:

```text
741205
```

وبالتالي لم يعد الكود مرتبطًا مباشرة بمنتج واحد، وإنما بالعميل نفسه.

---

## 8. هل Access Code مرتبط بالدورة؟

**لا.**

في هذا التصميم، الـ Access Code مرتبط بالعميل، وليس بالدورة.

مثلًا:

```javascript
courses: ["step", "english"]
```

هذا يعني أن العميل يستطيع استخدام **نفس الكود** للوصول إلى كل الدورات الموجودة في مصفوفة `courses` الخاصة به.

---

## 9. إضافة عميل جديد

### الخطوة 1 — إنشاء ملف العميل

أنشئ ملفًا جديدًا:

```text
customers/212345681.js
```

### الخطوة 2 — إضافة بيانات العميل

```javascript
window.YAZEED_CUSTOMER = {
    orderNumber: "212345681",
    accessCode: "482915",
    courses: [
        "writing"
    ]
};
```

### الخطوة 3 — إضافة العميل إلى `map.js`

```javascript
window.YAZEED_CUSTOMER_FILES = {
    "212345678": "212345678.js",
    "212345679": "212345679.js",
    "212345680": "212345680.js",
    "212345681": "212345681.js"
};
```

بعد ذلك يصبح العميل الجديد جزءًا من نظام التفعيل.

---

## 10. عميل يشتري دورة إضافية

إذا اشترى العميل دورة إضافية، يمكن **الإبقاء على نفس Access Code** وإضافة الدورة فقط.

### قبل

```javascript
courses: ["step"]
```

### بعد

```javascript
courses: ["step", "english"]
```

وبذلك لا يحتاج العميل بالضرورة إلى Access Code جديد.

---

## 11. تغيير Access Code لعميل

يمكن تغيير الكود مباشرة داخل ملف العميل.

### الكود الحالي

```javascript
accessCode: "583921"
```

### الكود الجديد

```javascript
accessCode: "812440"
```

وبذلك يصبح:

```text
812440
```

هو الكود المعتمد في النظام لهذا العميل.

---

## 12. العلاقة مع `/activate`

تدفق عملية التفعيل يكون كالتالي:

```text
hub.yazeedenglish.com/activate
             ↓
       Order Number
             ↓
        Access Code
             ↓
           map.js
             ↓
        ملف العميل
             ↓
   مطابقة Access Code
             ↓
          Consent
             ↓
    حفظ التفعيل محليًا
             ↓
       Hub / الدورة
```

---

## 13. لا حاجة لإدخال البيانات يوميًا

بعد نجاح التفعيل، يحتفظ `localStorage` بحالة الوصول لمدة المدة المحددة في:

```text
activate/script.js
```

وطالما أن مدة التفعيل لم تنتهِ، فلن يحتاج العميل إلى إدخال:

- Order Number
- Access Code

كل يوم.

---

## 14. مدة التفعيل

القيمة الحالية:

```javascript
const ACCESS_DURATION = 30 * 24 * 60 * 60 * 1000;
```

### 100 يوم

```javascript
const ACCESS_DURATION = 100 * 24 * 60 * 60 * 1000;
```

### 365 يومًا

```javascript
const ACCESS_DURATION = 365 * 24 * 60 * 60 * 1000;
```

يمكن تغيير الرقم الأول فقط حسب المدة المطلوبة.

---

## 15. شروط رقم الطلب

### الشرط الحالي

```javascript
return /^\d{9}$/.test(value);
```

ولشرط **9 أرقام تبدأ بالرقم 2**:

```javascript
return /^2\d{8}$/.test(value);
```

### المعنى

يجب أن يكون رقم الطلب:

- 9 أرقام بالضبط.
- يبدأ بالرقم `2`.

### إلغاء شرط البداية

إذا كان المطلوب قبول أي رقم مكون من 9 أرقام:

```javascript
return /^\d{9}$/.test(value);
```

### السماح فقط بالأرقام التي تبدأ بـ 3

```javascript
return /^3\d{8}$/.test(value);
```

---

## 16. إزالة النص داخل حقول الإدخال

إذا كان لديك:

```html
placeholder="مثال: 212345678"
```

يمكن حذفه بالكامل.

ليصبح الحقل:

```html
<input type="text" id="orderInput" maxlength="9">
```

وبالمثل، إذا كان لديك:

```html
placeholder="6 أرقام"
```

احذفه:

```html
<input type="password" id="accessCodeInput" maxlength="6">
```

> **ملاحظة:** `placeholder` مجرد نص إرشادي يظهر داخل حقل الإدخال، وليس جزءًا من البيانات نفسها.

---

## 17. الحفاظ على باقي المشروع

لا توجد حاجة إلى إعادة بناء الـ **Hub** أو الدورات الأربع لمجرد الانتقال من Access Codes ثابتة إلى Unique Access Codes.

التعديل الأساسي يكون داخل:

```text
/activate
/customers
نظام قراءة بيانات العميل
```

### يجب أن يبقى كما هو

- `Consent`
- `localStorage`
- مدة الوصول
- `Lock`
- `Purchase button`
- إظهار جميع المنتجات للتسويق
- `Course Guard`
- STEP كما هو
- English / Trab6 / Writing كقارئ واحد مع تغيير ملف PDF فقط

---

## 18. Frontend-only Limitation

من المهم فهم أن هذا النظام **Frontend-only**.

جميع بيانات العملاء موجودة في ملفات Frontend يمكن للمستخدم الوصول إليها أو تحليلها، مثل:

```text
customers/212345678.js
customers/212345679.js
customers/212345680.js
```

لذلك:

> **Unique Access Codes تحسن تنظيم النظام وفصل بيانات العملاء، لكنها لا تجعل النظام High Security.**

أي مستخدم تقني لديه القدرة على فحص ملفات JavaScript أو موارد الموقع قد يستطيع الوصول إلى بيانات العملاء أو تحليل طريقة التحقق.

وبالتالي هذا التصميم مناسب كنظام **تنظيم وتفعيل Frontend-only**، وليس كنظام أمان حقيقي يعتمد على سرية البيانات.

---

## 19. ملاحظة حول عدد العملاء

هذا الأسلوب مناسب عندما يكون عدد العملاء:

- صغيرًا.
- متوسطًا.
- ويمكن إدارته يدويًا.

لكن إذا أصبح عدد العملاء ضخمًا جدًا، فإن وجود:

```text
ملف مستقل لكل عميل
+
map.js
```

سيصبح أكثر صعوبة في الإدارة والصيانة.

ومع ذلك، فهذا خارج نطاق التصميم الحالي الذي يشترط بقاء المشروع **Frontend-only**.

---

## 20. الهدف من هذا التنظيم

الفكرة الأساسية هي فصل المسؤوليات بشكل واضح:

```text
┌──────────────────────────────┐
│        Hub Files             │
│   الواجهة والمنطق العام      │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│       Activate Files         │
│ التحقق + Consent + Activation│
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│      Customers Files         │
│       بيانات العملاء فقط     │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│       Course Files           │
│        محتوى الدورات         │
└──────────────────────────────┘
```

### النتيجة

هذه البنية تجعل:

- إضافة عميل جديد مستقلة عن بقية المشروع.
- تعديل دورات العميل عملية بسيطة.
- تغيير Access Code لعميل معين سهلًا.
- إدارة العملاء أكثر تنظيمًا.
- منطق التفعيل منفصلًا عن بيانات العملاء.
- محتوى الدورات منفصلًا عن نظام العملاء.
- المشروع محافظًا على بنية **Frontend-only** الحالية.

---

## الخلاصة

التحول من:

```text
Product
   ↓
Fixed Access Code
```

إلى:

```text
Order Number
   ↓
Customer File
   ↓
Unique Access Code
   ↓
Customer Courses
```

يتيح لكل عميل امتلاك **كود خاص به** يمكنه فتح جميع الدورات المخصصة له، مع إمكانية إضافة دورات جديدة أو تغيير الكود دون التأثير على بقية العملاء.

والبنية النهائية المقترحة هي:

```text
hub.yazeedenglish.com/
│
├── index.html
├── style.css
├── script.js
├── access-guard.js
│
├── activate/
│   ├── index.html
│   ├── style.css
│   └── script.js
│
└── customers/
    ├── map.js
    ├── 212345678.js
    ├── 212345679.js
    ├── 212345680.js
    └── ...
```

**المبدأ الأساسي:**  
> **ملف العميل يحتوي على البيانات فقط، بينما نظام التفعيل والمنطق العام يبقيان منفصلين عنه.**